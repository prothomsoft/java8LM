var findNumberOfWomanNotShorterThan = function(people, minimalHeight) {

    //Just like in Java except instead of lambda an inline function has to be used
    //Btw, remember also that custom classes has to be "imported" before an usage

//    return people.stream().
};
